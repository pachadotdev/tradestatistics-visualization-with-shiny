% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in utils_server.R
\name{partner_top_chart}
\alias{partner_top_chart}
\title{Top-partner bar chart (top partners + optional highlighted partner + rest of the world)}
\usage{
partner_top_chart(d, title, bar_color, highlight_code = NULL, lookup_dt)
}

\arguments{
\item{d}{input data.table with columns `partner` (dynamic country code) and `trade` (raw trade value)}

\item{title}{chart title}

\item{bar_color}{bar color}

\item{highlight_code}{dynamic code of the selected partner to always show separately, or `NULL`}

\item{lookup_dt}{data.table with columns `code`/`country`, as built from `flatten_countries()`}
}

\description{
Builds a bar chart of a reporter's top trading partners. With `highlight_code`
left as `NULL` this is a plain top 4 + "Rest of the world" breakdown (multilateral view). When
`highlight_code` is supplied (bilateral view), it shows the top 3 *other* partners plus the
selected partner shown separately (labelled "(selected)"), so it can be compared against the
top partners regardless of its own rank.
}

